import { Vue } from 'vue-property-decorator';
export default class Polkadot extends Vue {
    private size;
    private iconProperties;
    readonly generateCircles: string;
    private renderCircle;
}
